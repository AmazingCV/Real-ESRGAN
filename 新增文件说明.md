# 红外图像训练工具 - 已创建文件总结

## ✅ 已创建的文件

### 🔧 核心工具（3个）

1. **`estimate_degradation_params.py`** - 退化参数估计工具
   - 分析LR/HR图像对
   - 自动估计训练参数范围
   - 生成可视化分析图表
   
2. **`prepare_infrared_training.py`** - 一键准备训练工具
   - 自动化整个准备流程
   - 创建目录结构
   - 生成配置文件
   
3. **`example_usage.sh`** - 使用示例脚本
   - 各种场景的使用示例
   - 快速开始指南

### 📋 配置文件（1个）

4. **`options/train_realesrgan_x4plus_infrared.yml`** - 红外图像训练配置模板
   - 包含详细的参数说明
   - 标记需要修改的地方
   - 红外图像特殊优化建议

### 📚 文档（4个）

5. **`INFRARED_TRAINING_GUIDE_CN.md`** - 完整训练指南
   - 从准备到训练的完整流程
   - 两种场景（有/无LR图像）
   - 常见问题解答

6. **`DEGRADATION_ESTIMATION_CN.md`** - 参数估计工具详解
   - 估计原理和方法
   - 使用方法和示例
   - 进阶使用技巧

7. **`README_INFRARED_CN.md`** - 工具索引和快速开始
   - 所有工具的功能说明
   - 快速开始命令
   - 常用命令速查

8. **`新增文件说明.md`** - 本文件
   - 文件列表和说明
   - 快速开始步骤

---

## 🚀 快速开始（3步走）

### 如果你有LR和HR图像对（推荐）✨

```bash
# 第1步：一键准备（自动分析参数）
python3 prepare_infrared_training.py \
    --hr_dir /path/to/your/hr_images \
    --lr_dir /path/to/your/lr_images \
    --output_dir ./datasets/infrared \
    --channels 3  # 灰度图用1，RGB用3

# 第2步：查看分析结果
# - 终端会显示推荐的参数范围
# - degradation_analysis.png 包含可视化分析
# - 根据建议调整 datasets/infrared/train_config.yml

# 第3步：开始训练
python3 realesrgan/train.py -opt datasets/infrared/train_config.yml
```

### 如果只有HR图像

```bash
# 第1步：准备目录（手动设置参数）
python3 prepare_infrared_training.py \
    --hr_dir /path/to/your/hr_images \
    --output_dir ./datasets/infrared \
    --channels 3 \
    --no-estimate

# 第2步：手动编辑配置文件
# vim datasets/infrared/train_config.yml
# 根据你对红外图像的了解，修改退化参数

# 第3步：开始训练
python3 realesrgan/train.py -opt datasets/infrared/train_config.yml
```

---

## 📖 查看详细文档

根据你的需求选择阅读：

| 需求 | 阅读文档 |
|------|----------|
| **快速上手，完整流程** | `INFRARED_TRAINING_GUIDE_CN.md` ⭐推荐 |
| **理解参数估计工作原理** | `DEGRADATION_ESTIMATION_CN.md` |
| **查看使用示例** | 运行 `./example_usage.sh` |
| **了解配置参数含义** | `options/train_realesrgan_x4plus_infrared.yml` |
| **工具功能速查** | `README_INFRARED_CN.md` |

---

## 🎯 核心解决方案

### 你的问题：
> "训练时只有HR图像，退化参数是范围值，逐个测试太慢，想知道参数大概在哪个范围"

### 解决方案：

#### ✅ 如果你有几张LR/HR图像对：

使用 **`estimate_degradation_params.py`** 工具：

```bash
python3 estimate_degradation_params.py \
    --hr_dir ./your_hr_images \
    --lr_dir ./your_lr_images \
    --visualize
```

**输出示例**：
```
推荐的配置文件参数:
====================================
blur_sigma: [0.3, 2.1]      # 模糊核标准差
noise_range: [5, 25]        # 高斯噪声范围
poisson_scale_range: [0.25, 2.50]  # 泊松噪声范围
jpeg_range: [45, 85]        # JPEG压缩质量范围
...
```

直接把这些值填入配置文件即可！🎉

#### ✅ 如果没有LR图像：

根据红外图像质量选择预设参数：

**高质量红外图像（轻度退化）**：
```yaml
blur_sigma: [0.2, 1.0]
noise_range: [1, 15]
jpeg_range: [60, 95]
```

**低质量红外图像（严重退化）**：
```yaml
blur_sigma: [0.5, 3.0]
noise_range: [5, 30]
jpeg_range: [30, 70]
```

详见 `INFRARED_TRAINING_GUIDE_CN.md` 的"方案二"部分。

---

## 💡 工具特点

### 1. estimate_degradation_params.py

**分析方法**：
- ✓ 模糊检测：拉普拉斯方差 + 频域分析
- ✓ 噪声检测：中值绝对偏差(MAD)
- ✓ JPEG质量：块效应检测
- ✓ 可视化：生成直方图和对比图

**输出**：
- 每对图像的详细分析
- 所有图像的统计汇总
- 可直接使用的参数建议
- 可视化分析图表

### 2. prepare_infrared_training.py

**自动化流程**：
1. ✓ 检查依赖和数据
2. ✓ 创建目录结构
3. ✓ 生成meta_info.txt
4. ✓ 运行参数估计（可选）
5. ✓ 创建配置文件
6. ✓ 显示下一步指南

**智能匹配**：
- 自动匹配LR/HR图像对
- 支持不同命名规则
- 批量处理多张图像

---

## 🔍 关键参数说明

训练配置中最重要的退化参数：

| 参数 | 含义 | 典型范围 |
|------|------|----------|
| `blur_sigma` | 第一次模糊的标准差 | [0.2, 3.0] |
| `blur_sigma2` | 第二次模糊的标准差 | [0.2, 1.5] |
| `noise_range` | 高斯噪声强度范围 | [1, 30] |
| `noise_range2` | 第二次高斯噪声强度 | [1, 25] |
| `jpeg_range` | JPEG压缩质量范围 | [30, 95] |
| `jpeg_range2` | 第二次JPEG压缩质量 | [30, 95] |

**红外图像特殊参数**：
```yaml
gray_noise_prob: 0.6      # 增加灰度噪声概率
gaussian_noise_prob: 0.6  # 增加高斯噪声概率
sinc_prob: 0.05           # 降低sinc滤波（如果模糊少）
```

---

## ⚙️ 环境要求

### 必需依赖（训练）
```bash
pip install torch torchvision basicsr
```

### 可选依赖（参数估计）
```bash
pip install opencv-python numpy scipy scikit-image matplotlib
```

### GPU要求
- 推荐：NVIDIA GPU with 8GB+ VRAM
- 最低：NVIDIA GPU with 6GB VRAM（需降低batch_size）

---

## 📊 使用流程图

```
开始
  │
  ├─ 有LR/HR图像对？
  │   │
  │   ├─ 是 ──→ prepare_infrared_training.py (带--lr_dir)
  │   │         └─→ 自动估计参数 ──→ 查看结果
  │   │                           └─→ 微调配置
  │   │                               └─→ 开始训练
  │   │
  │   └─ 否 ──→ prepare_infrared_training.py (--no-estimate)
  │             └─→ 手动设置参数
  │                 └─→ 开始训练
  │
训练完成
  │
  └─→ 使用模型推理
```

---

## 🎁 额外功能

### 1. 可视化分析图表

运行带 `--visualize` 参数时，会生成包含以下内容的图表：
- 模糊程度分布直方图
- 噪声水平分布直方图
- JPEG质量分布直方图
- HR vs LR 高频能量对比图

### 2. 批量处理

工具支持批量分析多对图像，自动计算统计信息。

### 3. 智能建议

根据分析结果，自动给出：
- 参数范围建议
- 红外图像特殊优化建议
- 通道数设置建议
- 缩放比例建议

---

## 📝 示例输出

运行 `estimate_degradation_params.py` 后的典型输出：

```
============================================================
分析图像对:
  HR: ./hr/image_001.png
  LR: ./lr/image_001.png
============================================================

1. 缩放比例分析:
   平均缩放: 4.00x
   高度缩放: 4.00x
   宽度缩放: 4.00x

2. 模糊程度分析:
   HR图像 - 拉普拉斯方差: 245.67, 高频比例: 0.3245
   LR图像 - 拉普拉斯方差: 89.23, 高频比例: 0.1567
   估计的模糊sigma: 1.68

3. 噪声水平分析:
   HR图像噪声: 3.21
   LR图像噪声: 12.45
   LR额外噪声: 9.24

4. JPEG压缩质量分析 (LR图像):
   块效应强度: 3.67
   估计JPEG质量: 65

============================================================
汇总分析结果与参数建议
============================================================

推荐的配置文件参数:
====================================

# 第一次退化过程
blur_sigma: [0.8, 2.2]
noise_range: [5, 18]
poisson_scale_range: [0.25, 1.80]
jpeg_range: [52, 78]

# 第二次退化过程
blur_sigma2: [0.4, 1.1]
noise_range2: [4, 14]
...
```

---

## ❓ 常见问题

### Q: 我的图像是16位的，可以用吗？
**A**: 可以！工具自动支持16位图像。

### Q: 需要多少张LR/HR图像对来估计参数？
**A**: 建议至少3-5对，越多越准确。

### Q: 估计的参数不准确怎么办？
**A**: 可以手动微调。估计结果是参考，最终需要根据训练效果调整。

### Q: 可以只分析参数，不准备训练吗？
**A**: 可以！单独运行 `estimate_degradation_params.py`。

---

## 🎓 下一步

1. **阅读完整指南**：`INFRARED_TRAINING_GUIDE_CN.md`
2. **准备你的数据**：组织HR（和LR）图像
3. **运行准备脚本**：`prepare_infrared_training.py`
4. **开始训练**：`realesrgan/train.py`
5. **监控和优化**：根据结果调整参数

---

## 🙏 总结

现在你有了：
- ✅ 自动参数估计工具
- ✅ 一键准备训练脚本
- ✅ 优化的红外图像配置模板
- ✅ 详细的使用文档和示例
- ✅ 完整的训练指南

**不再需要"大海捞针"式地测试参数！** 

工具会帮你快速确定合适的参数范围。🎯

祝训练顺利！🚀
